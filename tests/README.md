# Test Repositories

Place test repositories here for validation.

Example:
```bash
cd tests
git clone https://github.com/pradal/ligo-binder
cd ligo-binder
python -m coderevival analyze . --generate-config
```
